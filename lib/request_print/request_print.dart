import 'package:flutter/material.dart';

class RequestPrint extends StatelessWidget {
  const RequestPrint({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Request Print'),
      ),
    );
  }
}
