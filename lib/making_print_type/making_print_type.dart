import 'package:flutter/material.dart';

class MakingPrintType extends StatelessWidget {
  const MakingPrintType({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Making Print Type'),
      ),
    );
  }
}
