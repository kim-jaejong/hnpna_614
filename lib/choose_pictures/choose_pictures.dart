import 'package:flutter/material.dart';

class ChoosePictures extends StatelessWidget {
  const ChoosePictures({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Choose Pictures'),
      ),
    );
  }
}
