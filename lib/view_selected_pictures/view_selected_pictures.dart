import 'package:flutter/material.dart';

class ViewSelectedPictures extends StatelessWidget {
  const ViewSelectedPictures({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('View Selected Pictures'),
      ),
    );
  }
}
